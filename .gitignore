# secrets in mjs are a bit different
config.mjs
!example_config.mjs

# all the local build output
/build

# example json output
resp.json

#Node dependencies
node_modules/

#Build artifacts (we only commit the template)
manifest.json

# Jest cache
.jest-cache